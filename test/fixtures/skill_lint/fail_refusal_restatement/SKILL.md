---
name: plastic-fail_refusal_restatement
description: Fixture skill that restates a refusal rule the conventions chapter already carries.
user-invocable: true
---

# Fail Refusal Restatement

A pre-flight lock guard refuses to proceed when a live foreign session already holds the
intent lock, so back off and wait rather than forcing the write.
