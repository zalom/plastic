# Skeptic report: performance/language claims (skeptic-perf)

Verdicts: (1) 854ms/edit CONFIRMED as wall clock, mechanism WRONG: Claude Code runs matching hooks IN PARALLEL (docs: "runs all matching hooks in parallel, deduplicates identical commands"); measured 5 PreToolUse concurrent = 430-623ms + serial PostToolUse gate-check 249-273ms = 703-872ms total; the cost is CPU contention among 5 simultaneous Ruby starts, not summation (serial sum would be 966ms; per-hook table in raw report sums to 2,177ms and was never reconciled). Consequence: "X ms/spawn x N" savings arithmetic over-counts ~N-fold. --disable-gems saves 57ms/spawn but ~106ms/edit (not 390ms). (2) 970->251 CONFIRMED numerically, conservative even (measured: today 703ms; no shims 248ms; +--disable-gems 142ms; single merged process floor 56ms). --disable-gems VERIFIED SAFE: all 17 hooks + doctor/plastic-lock/end-intent/new-intent/dashboard/agent-report/validate-intent/qmd-sync load clean (doctor.rb:17 and preflight.rb:4 explicitly require "rubygems" back on demand). BUT deleting shims is effort M not S: shims carry (a) 9ms bash pre-guard `[ -f ~/.plastic/INDEX.md ] || exit 0` saving a Ruby spawn for every NON-Plastic user on every tool call, (b) installer path rebinding (installer_core.rb:637), (c) 4 scripts read ARGV not stdin (code-gate, lock-gate, savepoint-pre, gate-check) = rewriting 4 entrypoints + tests. (3) qmd probe CONFIRMED (hook-retrieval-gate:135 calls detect_capabilities BEFORE run/classify; no cache exists; probe = 98% of hook: 590ms with qmd, 13ms without). Better fix than cache: LAZINESS - move detect_capabilities inside the classify(path)==:qmd branch (retrieval_gate.rb:69-71); most reads never target a store, pay zero; no TTL/invalidation machinery. (4) Stay-on-Ruby CONFIRMED on latency, MISSING-CONTEXT on distribution: preflight RUBY_FLOOR=3.0.0 but stock macOS ships ruby 2.6.10 -> zero-to-one install failure on a clean Mac, landing exactly on the marketplace channel (GTM #1). Standard options first: test whether scripts run on 2.6 and lower the floor (untested!); npm postinstall ruby bootstrap; document one-line mise install. "Rewrite sheds incidental complexity" = untestable optimism; 80/128 test files require_relative the modules directly (rewrite discards ~62% of suite). Real question: minimum Ruby, toolchain-free install - not Go vs Ruby. (5) "Suite protects against concurrency defects" CIRCULAR (1,836/1,837 tests don't exercise concurrency) and supporting claim FALSE: scripts/write-config:64 holds flock LOCK_EX + temp-rename, test/write_config_test.rb:121-136 is a genuine threaded-subprocess race test. KEY REFRAME: Plastic already solved atomic locking correctly once; lock.rb fix = "port the write-config pattern" with in-repo test template. (6a) SessionStart wipes armed bridge CONFIRMED (derive builds virgin hash :788/:805/:812, writes :820 unconditionally; no armed check; stable session key; matcher "" fires on startup/resume/clear/compact). (6b) Store-path bug REAL but report's LOCATION AND FIX WRONG: hook-session-start:52 appends /store itself, so changing the shim arg to ~/.plastic/store yields store/store/ and BREAKS it; actual defect is store_root passed as store: to Bridge.derive whose :779 sub leaves dir="store/28--...". Fix belongs in hook-session-start:52-54. Live: exactly 3 malformed bridges, all intent 28 (the lone global Active), under 3 session ids.

## Missed findings (KISS-first)
M1. CREATE GATE DEAD FOR PLUGIN-MARKETPLACE USERS: hooks/create-gate committed mode 100644 (non-executable); plugin path run-hook bare-execs it -> Permission denied exit 126 on every Write/Edit; works locally only because npm installer chmods (installer_core.rb:640). Invalidates a claimed strength for exactly GTM channel #1. One-character fix (git update-index --chmod=+x).
M2. Worst hook is the PROMPT path, mis-measured 7x: qmd-search skips search under 10 chars or "continue" (qmd_hook.rb:35, MIN_PROMPT_LENGTH); first report's 154ms = the skipped path. Real prompts: 1,174ms / 1,024ms (Timeout 2s bound). Full prompt path parallel = 919ms, and UserPromptSubmit delays the FIRST TOKEN (felt), while edit hooks hide behind a spinner. Corrected perceived ranking: prompt (919ms felt) > boot (1,020ms once) > edit (703-872 hidden). ~1,000ms off every real prompt = top user-perceived win, absent from the report.
M3. Gate telemetry exists, unopened: ~/.plastic/.cache/gate-escapes.log = 756 escapes, 16 sessions, one month (2026-07), by the author. No denial log exists anywhere -> system cannot answer "which gate ever blocked something that mattered". Cheaper than optimizing 5 gates: add one-line denial logging, re-read in 30 days, DELETE gates that never fired.
M4. Retrieval gate: deletion simpler than optimization. Advisory by construction, 590ms x ~150 calls = ~88s/delivery (over half the review's "minute of hook latency"), and the same guidance ships twice elsewhere (UserPromptSubmit power-tools line + AGENTS.md). Deleting is a smaller diff than lazy-loading; 13ms x 150 is still one more moving part than zero.
M5. PLASTIC.md is 53,558 B (~13.4k tok) not 55,558/13.9k; wrong figure repeated 3x in synthesis (transcribed, not recomputed).

## Corrected savings table
Lazy/skip qmd probe: 577ms/retrieval (laziness beats cache). --disable-gems: ~106ms/edit. Delete shims: ~455ms/edit but effort M + removes non-user bail-out. Merge 5 hooks into 1: floor 56ms, SUBSUMES both prior fixes. Fix prompt-path qmd-search: ~1,000ms/prompt, top felt win (not in report).
Bench scripts: /Users/zlatko/.claude/jobs/5b84ee0b/tmp/bench.rb, bench2.rb.
