# Skeptic report: workflow claims (skeptic-workflow)

Headline: 4 of 7 challenges land, 2 fail, 1 exposes a factual error changing the headline number. The report measured "medium" delivery with tier-L topology (7 sessions); shipped M topology is 5 sessions (tiers.md:29-40) -> 78x should be ~61x.

1. 78x/strawman: PARTLY CONFIRMED. Topology error (above). Caching does NOT rescue the challenge: prefix caches are model+prompt-scoped so the 7 differing agents can't share; the report charged boot once/session which caching can't go below (and a cache WRITE bills 1.25x, making the report 25% too LOW); 78x is context-occupancy, caching reduces dollars not occupancy. Cost recompute (Opus $5/$25, Sonnet intro $2/$10, 25 turns/session): M boot bill $1.39/delivery, L $1.90; PLASTIC.md split saving $0.57/delivery, ~$200 across all 354 real deliveries ever. Denominator problem: honest marginal ratio incl. ~300k real work tokens is ~1.5x. Typo strawman: partly - 354 real deliveries, 43% have zero action files, 30% <=6 checklist items, so small work does route through the lifecycle; but savepoint ledgers max at 15 entries ever (median 9): nothing the system records resembles "52 steps".

2. Delete report contracts/briefings: WRONG. scripts/agent-report:5-17 is a documented FALLBACK ("when a dispatched agent returns no usable report"), a pure function of the dir; it can't report what the specialist judged - reading it as proof of redundancy inverts the inference. Mandated review output caught real defects: store 202 outcome.md:39-44 (Opus gate review caught 3 defects incl. date parser hiding 83% of history from next-work ranking); store 96 intent:197-199 (reviewer caught per-session skeleton-key lock bug). Human briefings are recorded owner preferences (EM-to-CTO, review-before-merge); intent 177 exists solely to protect that human decision point.

3. Skill merges/deletions: OVERSTATED. Byte counts don't reproduce (humanizer SKILL.md 2,287 B not 4,765; skill-evaluating 5,996 B body / 19,234 dir not 15,339) so the 33,824 B and 47% totals are unreconstructable. Humanizer is HOW the no-AI-tells rule gets applied (ships always-on-snippet.md); moving it to CLAUDE.md worsens the always-on budget. Skill-evaluating enforces an owner ruling from intent 166 (every changed skill ships evals). Intent-grilling is user-invoked by name ("grill me") with tuned style. Install-family merge inverts: claimed saving implies a 4-op skill smaller than install alone, and any trigger would load all four ops' prose; always-on saving is ~300 B of descriptions. KISS cuts against this merge.

4. New CLI verbs vs anti-machinery thesis: CONFIRMED tension, and ALREADY ANSWERED IN THE STORE. Solo fallback already ships (auto/SKILL.md:185 single agent walks the full cycle). Intent 213 (snappy-intent-delivery) holds a Fable advisor verdict (2026-07-18, resources/advisor-verdict-and-design-state.md, 8,833 B) covering bundle scripts, hook-layer code-before-How enforcement (same bridge.rb:1105 line), Tier-line expression; winning rival: "scripts only, no new concepts - convert hot fixed procedures into scripts one at a time, use existing Tiers for speed." Batch 4 should RESUME intent 213's three open decisions, not re-plan.

5. Folgezettel replaces graph: WRONG. Of 504 edges, only 105 (21%) derivable from ID ancestry; 399 (79%) carry non-ID information. (Also ls 1* matches 134 dirs - needs segment parsing.) QMD live test: flat search found the tier-invariant decision at 0.89 relevance - adequate when the agent knows to ask; real gap is pull vs push. Context routing SURVIVES this challenge.

6. Tier same-structure invariant: CONFIRMED steelman. tiers.md:57-63 states the reason (per-tier gate logic + broken state derivability) and a named follow-up path - the design is already honest. Report scoring self-contradicts: 2 fewer dispatches = ~40k tokens = ~29% of the boot bill on the axis the report itself calls dominant (77% of overhead), yet tier is graded down on axes it declined for stated reasons. Intent 130's target was work-time ratio, never gate count.

7. Missed by first report:
- Largest cost lever is one config line: agents.models.plastic-brainstorming: fable ($10/$50). One Fable brainstorming session ~$3.34 = 2.4x the whole 5-session boot bill ($1.39), ~6x the PLASTIC.md-split saving ($0.57). Report never opened config.yml. (Caveats: per-agent model/effort is user config not a defect; standing ruling protects the ADVISOR on cost - brainstorming is a separate key but may be deliberate.)
- Owner-config findings should be reported as config changes (stage-per-bg-agent is shipped default PLASTIC.md:157 + owner preference; "change your config" beats "change Plastic").
- Reducing the NUMBER of lifecycle stages is never examined (presumably product thesis; say so explicitly).
- harness-adapters.md:117 vs :126 is NOT a contradiction (L1 file injection vs L2 hook event, two mechanisms); re-verify before quoting the 42x/78x split.

Net effect on batches: 1 Snappy untouched (strongest part). 2 Safe untouched (highest value). 3 Lean WEAKENED (split worth $0.57/delivery; contract deletion refuted; merge counts unreproducible). 4 Codified REDIRECT to resume intent 213. 5 Remembering STRENGTHENED. 6 Seen untouched.
