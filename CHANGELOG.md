# Changelog

Release history for Plastic, one line per cut. Commit-level detail lives in
[GitHub Releases](https://github.com/zalom/plastic/releases).

## Unreleased

## Released

- `2.0.0-alpha.15` - shipped 2026-09-05 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected the acceptance fixes of roadmap reporting-v2 (intent 331): 331a1 (the MessageDisplay launcher stakes a PENDING decision marker with shell builtins before Ruby boots, later chunks poll on it and the budget scales with chunk depth, so long screens such as the roster and the session report paint whole under real streaming; the concurrent replay harness joined the suite) and 331f1a (separator rows pass through the data-table fitter whole whenever they fit), plus two release fixes (the alpha.14 changelog line names the four doctor display checks; the plan-screen changelog pin reads the whole file). Per-intent detail in each intent's outcome.md.
- `2.0.0-alpha.14` - shipped 2026-09-05 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected roadmap reporting-v2 under intent 331: 331a (the MessageDisplay hook paints a screen wherever it sits in a reply; ScreenPaint kind registry), 331b (`report-screen plan`, the pre-delivery report), 331c (`report-screen roadmap plan|state|delivered`), 331d and 331d1 (`dashboard.rb --screen` with Graph ID and Intent columns), 331e (doctor `display` category: `display_hook_registered`, `display_hook_paints`, `display_not_defeated`, `display_surfaces_documented`, replayed through the installed launcher), 331f (every state-showing skill bound to its report verb, Report savepoint kind, column vocabulary, one lead freshness rule, 115-column bound), 331f1 (fitter never truncates labels or separators; roadmap state Intent column), 331f2 (title ends at the first colon followed by a space). Per-intent detail in each intent's outcome.md.
- `2.0.0-alpha.13` - shipped 2026-09-05 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 330 (a status ask prints the session: `report-screen session <tier_root>` prints one delivered screen per intent completed in the session window, oldest first, across every store the day ledger says the session touched, then the in-flight roster; the continuing and auto skills route every unnamed status ask through it; one shared fence walker now feeds both the heading splitter and the table reader, so a `#` comment inside a fenced code block no longer blanks a Proven-by cell or fabricates a test count; the ship identity on the delivered screen reads the project's `flow.base` instead of an `alpha` literal, so a jobext intent reads `1d1f8db -> main`; owner ruling 2026-09-04). Suite 2837 runs green on alpha.
- `2.0.0-alpha.12` - shipped 2026-09-04 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 329 (a checklist item is ticked with the commit that lands it: the tick convention now marks the box the Progress bar reads, the implementer prompt and the executor definition make commit, tick, and the savepoint Commit line one step, the enforcer and auto skill verify tick-versus-diff at review and at merge, and the doctor warns intent_ticks_lag when an intent has commits and nothing ticked; owner ruling 2026-09-04 after jobext 22 showed 0 of 9 ticked with four commits on its branch). Full suite 2775 runs green on alpha.
- `2.0.0-alpha.11` - shipped 2026-09-01 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected a direct fix to the delivered report screen: `report-screen delivered` read its version with `git describe` next to the script, which on an installed copy is `~/.plastic` (no tags) and in-repo is the tag nearest HEAD, so every post-delivery screen said `not recorded` or named the wrong release. The shipped version now comes from the record first (outcome.md's own "Shipped as", "released as", "Delivered in", "Release v" line) and, when the record is silent, from the lowest git tag that contains the intent's merge commit, in the project's repository resolved from `projects.yml` beside the store (`--repo <dir>` overrides). With neither, the screen still says `not recorded` rather than guessing from HEAD. The auto skill's two "Print the screen" steps now say the screen opens the reply with nothing before it and no code fence, the reason a screen pasted after prose rendered plain. Twelve new tests (seven readers, five CLI with a real tagged repo); tests landed red before the code. Full suite green on alpha.
- `2.0.0-alpha.10` - shipped 2026-09-01 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 317a1, the two owner findings on the alpha.9 screens: they painted the right layout in the wrong colors, and put field notes on the wrong line. The palette now follows intent 317's approved design files element by element. Evidence rows color by kind - teal for a proof (`suite`, `red`, `ship`, `doctor`, and the research-only `deposits` and `verdict`), amber for `deviates` - matched exactly, never as a substring. A note column is chosen by the table's own header, so Evidence's `Source` and Needs-you's `Why` render mid-grey while Delivered's `Proven by` stays plain, and a cell whose text is exactly `not recorded` reads as absent wherever it appears. The delay screen's two colors were attached to the wrong columns and are now where the design puts them: the timestamp plain, the event label carrying the hue, amber for a review and teal for a commit. The decisions line under `Asked` reads as a note. Field notes moved out of an indented second line into a padded third column on the field's own line, one implementation (`IntentScreenAnsi.field_table_lines`) that `ScreenPaint` delegates to so the two renderers cannot drift. The render width moved from 100 to 115 columns because the approved design measures 115: at 100 the note column collapsed to 16 to 19 columns on real records and would have ellipsized every note on the state screen the owner had just confirmed. Nothing is shortened to fit a column it was not measured for - a value or note that will not fit drops its own row back to the full-width own-line form. Two defects were fixed on the way rather than filed: a table row with more cells than its header raised `TypeError` instead of painting, reachable from any record whose prose contains a pipe, and a styled empty last cell hid trailing whitespace from `rstrip` on the line the reader sees. One adversarial plan review before the code, which forced the width and the fallback rules before anything was built, and one post-execution review after it, which caught a guard that had been rewritten in the same commit as the code it guards and was inert while the defect it named was live. Every fixed guard was mutation-tested afterwards. Full suite 2740 runs green.
- `2.0.0-alpha.9` - shipped 2026-08-31; collected 317a (report screens paint and read their own records: ScreenPaint seam in the shared core, MessageDisplay paints all four screens, forgiving record readers, labeled-table outcome contract, run_mode stamped at close, hollow-close gate at end-intent)
- `2.0.0-alpha.8` - shipped 2026-08-31 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 317, the three delivery report screens that replace prose reports. `scripts/report-screen` carries three verbs: `state <intent_dir>` is the mid-delivery report (the field table with a `Changed` row and the Steps table, every number from the record, never by eye; `state --all <store_root>` renders the roster across every in-delivery intent with a `Lead` line and collapsed blocks showing three open steps), `delivered` is the post-delivery report (Asked, Delivered with proof, Evidence, Needs you), and `delay` answers "why did X take so long" with the timeline, a derived `Where the time went`, and the Outcome line. Under them, `scripts/lib/report_screen.rb` reads only the record and says `not recorded` rather than guessing. Reviews and commits get their own savepoint kinds (`Savepoint.append_review_savepoint`, `append_commit_savepoint`, `scripts/savepoint-note`), and a lifecycle guard in all three stage readers (intent screen, spawn-preamble, agent-report) pins the stage pick to the five lifecycle stages so a Review or Commit line can never masquerade as the current stage. `templates/outcome.md` gains `## Needs you`; the auto, intent-continuing, intent-executing and intent-ending skills route state, delivered and delay questions to the screens. `--ansi` degrades to plain deliberately: 316a1's core exposes a record renderer, not a paint(text) seam, so painting the three report screens is named follow-up work for the owner. One adversarial plan review before the code and one post-execution review after it. Full suite 2652 runs green.
- `2.0.0-alpha.7` - shipped 2026-08-31 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 316a1, two owner rulings that arrived after 316a had completed. The TUI is now built for cross-harness use rather than for Claude Code: the shared core (`scripts/lib/intent_screen.rb`, `scripts/lib/intent_screen_ansi.rb`, `scripts/intent-screen`) carries no harness assumption, and the `MessageDisplay` handler, its CLI and its bash launcher are the named **Claude adapter**. The one coupling the audit found - `IntentScreenAnsi` stripped backticks and emphasis runs unconditionally, because Claude Code Markdown-processes `displayContent` even inside a raw ANSI block - became a `markdown_safe:` argument defaulting to false, which the adapter passes true at its single call site; the two core renderers now agree on the record's text, and a display surface that passes raw ANSI through does not inherit a concession it never needed. A consequence worth knowing: `scripts/intent-screen --ansi` on a real terminal now keeps backticks and asterisks where alpha.6 stripped them, which is correct for a surface with no Markdown pass. Both halves are named in every file header and in `docs/reference/harness-adapters.md`, pinned by three tests, one of which fails when a file adopts either phrase without being registered. New deposit `resources/evidence--harness-surfaces.md` in the intent record: a per-harness display-surface matrix for Claude Code and Codex, with the honest negative for Kimi and Hermes (intent 309 records their hook events and config locations and no display-surface data at all), so the owner can decide on a Codex adapter from evidence rather than from a guess - Codex has no display-substitution event, its only Plastic-to-user output channel is the hook dispatcher's stdout relay, and whether its TUI passes raw ANSI is honestly unprobed. The claudish coexistence question is recorded as a compatibility note, never a gate, per the owner's ruling. No behavior changed on the shipped Claude path: the styled block is byte-identical to alpha.6, proven by rendering the same record both ways. One adversarial plan review before the code and one post-execution review after it; the post-execution review mutation-tested every new pin and found two that passed trivially, both fixed before merge. Full suite 2535 runs green.
- `2.0.0-alpha.6` - shipped 2026-08-31 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 316a, the ANSI intent screen. The shared field code was fixed first so both renderers inherit it: an Insight row is now one clause in the value and a timestamp plus at most one clause in the note, each hard-truncated at a word boundary instead of spilling 750 characters of a mid-list remainder into the note column; the `What this means` heading no longer renders empty (its template tokens are gone, since nothing ever filled them); step text is truncated at a word boundary at 110 characters rather than cut mid-word; the `S1 — ` prefix strips when the separator is an em or en dash; and the Markdown pipe-escaping is now optional, so it never leaks into a styled block. New `scripts/lib/intent_screen_ansi.rb` renders the same public field methods as one ANSI block - bold near-white headings, a bold key column, teal `done` and amber `open` badges, a 24-cell eighth-block teal-on-graphite progress bar, a mid-grey note column (truecolor, never SGR 2 dim), every line capped at 100 visible columns and stripped of backticks and emphasis runs, because Claude Code still runs Markdown over a substituted block. `scripts/intent-screen` gains `--ansi`; the default stays the plain Markdown screen, and `NO_COLOR` or a non-TTY stdout emits the plain screen even with the flag. A `MessageDisplay` hook, registered for Claude Code only through `HookRegistry` (the Codex map is unchanged and pinned by test), recognizes a screen by its own `## ▶ <id> · ` first line - no marker line was added, so a user without the hook sees exactly today's output - resolves the intent directory through `StoreDiscovery` with cwd precedence, blanks the streamed chunks and substitutes the styled block spliced in place of the screen, keeping the prose the session writes around it. It fails open on everything: a failure at the final chunk re-emits the buffered original rather than leaving a blank message. Proven by live PTY capture of a real `claude` run, not by unit tests alone: teal, amber, graphite, mid-grey, near-white and bold all reach the terminal, no line wraps, and the trailing prose survives. Two defects only the live run could find were fixed on the way: Claude Code fires the per-chunk hook processes concurrently, so the buffer is now one atomically-written file per chunk index with a decision file written before any rendering and a bounded, injectable poll paid only by a chunk whose delta could belong to a screen; and the splice fallback, which had scanned to the last table row anywhere in the message, is bounded to the screen's own rows so it can never swallow an assistant's prose. One adversarial plan review before the code and one post-execution review after it. Full suite 2523 runs green.
- `2.0.0-alpha.5` - shipped 2026-08-30 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 315b, the seven defects the alpha verification (315) found on the real install, fixed in one intent, tests first: the capture hook writes a day-ledger line only for a prompt that asks for work, so questions, remarks, harness envelopes and bare continue/auto triggers stop being recorded as work, through a pure SessionLedger.capture_worthy? predicate biased to accept (A, superseding spec 298 D2(c) by owner ruling); the installed rollback.rb and update.rb no longer crash, because InstallerCore falls back to the VERSION file the installer actually copies when package.json is absent, and names both files when neither exists (B); session-commit cuts the subject at a word boundary and carries the full summary as the commit body, keeping the hard 72-character slice when the line has no boundary (C, amending spec 300 D5); doctor compares the agent-side VERSION for every installed harness as version_match_<key>, and reports stale Codex registrations over the union of expected and live event keys so a registration parked in a retired event is visible, both in the full tier only so the binary core tier on the session-start boot path is untouched (E); the dashboard parses completion dates and section ids from the wikilink form as well as the link form, through two merged regexes rather than one alternation, taking mihradesign from 0 of 70 dates to 70 while plastic holds at 276 with no date scraped from note prose (F); session start reads its stdin payload, tty-guarded, and prefers the payload session_id over the environment variable and then the pid, per spec 298 D1 (G); and doctor reports a .tmp/<session>/ directory that has a heartbeat but no pointer once past a short TTL, while hook-capture and hook-record both stop creating the directory and the heartbeat when no pointer exists, which is what produced the orphans (H). Flow-base detection picking main was ruled configuration rather than a defect and got no code. One adversarial plan review before the code and one post-execution review after it; the post-execution review, measuring the capture predicate against the owner's real prompt history, caught it rejecting 5 of 27 genuine work prompts and that was fixed before merge. Full suite 2460 runs green.
- `2.0.0-alpha.4` - shipped 2026-08-30 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 316, the intent screen: on continue, and on a state question about an intent, the session prints one Markdown block filled from the record by scripts/intent-screen (title from the INDEX line, a vertical field table of Store, Status, Stage, Savepoint, Progress with a 20-character bar, Next, Insight, each with a note; then the What-this-means bullets it writes itself; then a Steps table with S1..SN, done or open, and the item text), so no number is written by eye; the continue skill speaks the record names only (stage, not station; stale ledger, not drift; check off, not tick), the template ships in templates/intent-screen.md, and checklist items are Step N with S1..SN shorthand. Seventeen failure-mode tests. Full suite 2402 runs green.
- `2.0.0-alpha.3` - shipped 2026-08-30 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 315a, a defect found by 315 (the alpha.2 verification) on the real install: hook-capture read the prompt from `user_prompt` while Claude Code sends it under `prompt`, so no pending line was ever written and no direct work on alpha.2 was recorded in the day ledger; the hook now reads `prompt` with `user_prompt` as the fallback, six failure-mode tests cover both keys, their precedence, an empty prompt, and the continue and auto steering under the new key. Full suite 2385 runs green.
- `2.0.0-alpha.2` - shipped 2026-08-30 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 311, 312, and 313, batch 4 of the simplify-plastic roadmap (context handling in the ledger), which completes the seventeen intents of intent 296's design: every session writes a regenerable hand-off into the day directory (store/.sessions/<day>/handoff--<session>.md) at every ticked item, before a compaction, and at close, the PreCompact hook is a launcher over scripts/hook-savepoint whose message names the file (the 1.x block naming .plastic/INDEX.md is gone), and session start injects a bounded day summary (open items, the last five done, live auto intents with their latest savepoint line, other active sessions by heartbeat) instead of the raw ledger (311); the context_offer_tokens (350,000) and context_insist_tokens (500,000) config keys as absolute token counts with their templates/config.yml row, and the PLASTIC COMPACT block installed into ~/.claude/CLAUDE.md through its own marker pair, checked by doctor and stripped by rollback (312); bin/plastic-bench, an unshipped developer bench that boots a real install into a fixture home and reports the core block (8,059 of 8,192 bytes), the boot injection (9,240 of 15,000 bytes, byte-identical over five repeats), the skill catalog, and the doctrine working set with its gap against the ruled 15,000, with intent 223's ceilings kept (313). Full suite 2379 runs green.
- `2.0.0-alpha.1` - shipped 2026-08-30 on the alpha channel (install with `npx -y @zalom/plastic@alpha install --claude`); collected 297 to 309, the first cut of the simplify-plastic roadmap (intent 296's design): a session day ledger under store/.sessions/ with a per-session pointer and heartbeat, capture and record hooks, and a close hook that files the day (297 to 301); every write-time gate, the create gate, and the stage-transition gates removed on both harnesses (302); bridge.rb split into savepoint.rb, lock.rb, and arm.rb (303, 307); the intent tier, the spec header, and six stage agents cut with seventeen skill directories folded into their owners (304); PLASTIC.md rewritten under 200 lines with the doctrine budget tightened (305); the 44 MODIFY test files passed in place (306); auto on the new core with one enforcer and one executor per intent (307); end-intent backfills spec, plan, action, and outcome from the record and reports instead of refusing, plus doctor's backfilled_complete and session-ledger checks (308); the five-event hook map on both harnesses, Codex SessionEnd through a detached hand-off, and the power-tools hook retired (309); rollback strips its own hook registrations before handing off to an older package and targets every installed harness, update gains --yes, the dead install channel flags are gone, config_asks.yml ships (310). A 1.14 install reads none of the new store paths (dot-prefixed) and the rollback across the major is proven by test/rollback_major_test.rb against the real 1.14.1 installer and doctor; known alpha rollback wart: a `backfilled_complete` row in a store's doctor-exclusions file is unknown to 1.14.1 and makes its savepoint_operational check warn until the row is removed. Full suite 2225 runs green.
- `1.14.1` - shipped 2026-08-24; collected 289 (the check-update background npm call no longer holds the Codex dispatcher pipes, and the dispatcher stays silent on timeout; the CI flake on the check-update test is closed and the suite makes no live network call any more). Fix only.
- `1.14.0` - shipped 2026-08-24; collected 276 (doctor names a plastic- hook Plastic never registered and a Plastic registration whose launcher is gone from disk, on both harnesses; stray-skills check never vanishes and states the reserved prefix), 278 (uninstall prints every hook entry and statusline it removes; a corrupt statusline cache cannot abort it), 280 (dead doctor-exclusion rows reported and prunable via register-exclusions --prune with hold-backs), 281 (per-intent doctor savepoint check honors doctor-exclusions). 277 shipped inside the 1.13.0 tag. The check-update pipe-race fix (289) follows as 1.14.1.
- `1.13.0` - shipped 2026-08-24; collected 279 (statusline shows session usage and context instead of the active intent: context window used, 5h and 7d subscription meters with yellow at 70 and red at 90, session cost hidden at zero, claudish rewrites for this session read from the local claudish-to-english fork ledger; the bridge, savepoint and INDEX resolution left the script, and a render is one awk pass spawning 2 processes instead of 15; global intent 39 records the claudish install it reads from) and 277 (hooks_registered no longer passes on a SessionStart that carries only a retired launcher; merged to main earlier today from another session); full suite 2198 runs, 10015 assertions, 0 failures.
- `1.12.0` - shipped 2026-08-24; collected 275 (installer hook purge by registry: hook ownership decided by HookRegistry predicates on both harnesses instead of a plastic- substring match, nine retired launcher names recovered from registry history, every removed entry printed, the reserved plastic- prefix rule documented; closes the 1.11.0 update silently dropping a user-owned hook) and 274 (doctor exclusion index: a per-store plain-text doctor-exclusions file beside INDEX.md, a curated rule catalog shared with revisions tags, doctor skips knowingly-exempt intent-rule pairs with an honest count, and an owner-gated register-exclusions maintenance tool); full suite 2181 runs, 9948 assertions, 0 failures.
- `1.11.0` - shipped 2026-08-22; collected 269 (the plastic-humanizer skill and the writing-style directives removed from the shipped surface; the writing-style skill now lives in its own repo and loads through a session-start hook, global intents 32 to 32c) plus a test fix for codex 0.148.0's grammar layout; full suite 2123 runs, 9703 assertions, 0 failures.
- `1.10.0` - shipped 2026-08-19; collected 230/232/233 (atomic delivery-lock writes, two processes can no longer both own a delivery; bridge written once after lock and worktree settle; disarm no longer guesses between sibling intents), 235 (first install works on a clean Mac, RUBYOPT cleared at every spawn site), 228 (doctor core checks split into lib/doctor_core.rb so boot loads only its own checks), 229 (gates log one line per block so gate value is measurable), 236 (Tier S deliveries run cheap: tier-blind auto-mode demands gated by tier), 213 (deterministic lifecycle steps became scripts: spec/checklist/outcome scaffolds, one-call arm and report, bundled final-gate verdict), 249/250 (hook launchers actually ship to ~/.plastic/hooks, Codex live-state hooks stop failing open, registry-vs-STATE_HOOKS guard), 251 (Codex edit path 8 processes down to 1), 216 (Codex doctor drift check validates per-role model lines), 239 (Codex install speaks Codex, apply_patch grammar primary-sourced), 227 (one report per audience), and 266 with 238 and 252 (manifesto Plastic-changes section replacing the stale beta claim, docs truth sweep across 50 files: single-worktree and no-maintenance-lock corrections, real gate rosters and hook counts, phantom commands removed, uninstall store-deletion option removed, templates gain the Abandoned section, CLAUDE_PLUGIN_ROOT token replaced with ~/.plastic paths, installed-user doc pointers switched to GitHub links, tutorial verified with the gate roster fixed); full suite 2124 runs, 9706 assertions, 0 failures.
- `1.9.0` - shipped 2026-08-07; collected 223 (PLASTIC.md cut to a lean core, about 13.4k down to 4k tokens injected at every session start, deep doctrine moved to plastic-conventions skill chapters read on demand, plus a manifest-diff prune that removes renamed-away files on update), 226 (the per-read retrieval-gate hook deleted, ending a roughly 0.8s probe on every file read; the delivery-time reindex already keeps the index fresh), 244 (the five per-edit PreToolUse write gates merged into one edit-gates dispatcher, 11 Ruby processes down to 1 and edit-path hook latency 284ms down to 125ms, with a doctor check that every registered Claude hook is implemented), 246 (the per-prompt QMD hit search removed after 225 measured it losing to plain ripgrep, hook renamed to power-tools keeping only the power-tools recommendation line, about 0.7s back on every prompt), and the two evidence intents 224 (graph engineering research: work graphs read for ranking, never for context injection) and 225 (QMD value benchmark: agent-driven qmd query strongly valuable, per-prompt BM25 injection not); full suite 1864 runs, 6675 assertions, 0 failures.

- `1.8.0` - shipped 2026-07-19; collected 219 (doctor check-surface doctrine), 220 (core rescope: drift out of --core, project-path and global-store checks, guided failure route), 221 (store scan verifies Plastic-dependent ops, tool readiness checks, AGENTS.md Tools convention, install-wide full run), 221a (hermetic store-scan tests, CI green), 222 (doctor --intent per-intent scope, mechanical close gate exit 6), 211 (generic legacy-gap predicate, shipped amnesty file removed, packaging guard, maintenance-run rebuild-savepoint)

- `1.7.1` - shipped 2026-07-19; collected 218: fix the maintenance merge-back failing on hosts without a global git identity (CI runners, fresh machines) - the no-ff merge in scripts/lib/maintenance_git.rb now carries the same committer identity flags as the maintenance commit, proven by an identity-isolated regression test that disables git's OS-account fallback; clears the 3 CI test failures red since the v1.5.0 push; full suite 1796 runs, 6238 assertions, 0 failures.

- `1.7.0` - shipped 2026-07-19; collected 210: consolidated multi-harness updates - one update run from any agent now refreshes the shared core and every installed agent (per-agent install records as the registry, VERSION + manifest under <agent-dir>/plastic/ with Codex and Hermes gaining the record), per-agent transactional apply with snapshot and auto-restore on failed verification, same-version repair, harness-stamped versions.json rows, doctor version_match now fixable with a repair hint plus a new full-tier install_integrity advisory; full suite 1795 runs, 6231 assertions, 0 failures.

- `1.6.0` - shipped 2026-07-18; collected 184, 186, and 183: a read-only doctor advisory that warns when the installed Codex predates the v0.123.0 apply_patch hooks floor so gates never silently no-op, shipped per-role Codex model and effort defaults (gpt-5.6-sol high for reasoning roles, gpt-5.6-terra medium for executors, gpt-5.6-luna low; user-overridable via agents.models.codex.*), and the Boss-system reuse-mining verdicts that spawned the working-notes vault design intent (store-only); full suite 1763 runs, 6149 assertions, 0 failures.

- `1.5.0` - shipped 2026-07-18; collected 178, 197, and 204: retired Plastic's unused per-intent store worktrees in favor of the harness-native code worktrees (Claude and Codex both ship their own) with a one-time surgical orphan sweep that spares any branch ahead of main, separated WORK (frozen once terminal) from MAINTENANCE (allowed on terminal intents only through a recorded append-only revisions.md receipt) and scoped end-intent's store commit to the completing intent's own paths so a concurrent session's completion can no longer sweep another session's uncommitted work with git add -A, and made doctor derive its Claude hook-launcher checks from the HookRegistry instead of a hand-kept list; full suite 1754 runs, 6127 assertions, 0 failures.

- `1.4.1` - shipped 2026-07-15; collected 198, 199, 200, 201, 202, 203, and 108a: Codex installation and live-state reliability, harness-aware skill invocation, doctor verification of implemented hooks, the shell-tool gate repair, a shorter project dashboard, and durable lock-owner provenance with the `who` view and dashboard worker activity; full suite 1704 runs, 5898 assertions, 0 failures.

- `1.4.0` - shipped 2026-07-14; collected the doctor-consistency roadmap (195), which closed ten findings that shared one cause: Plastic gated the front of the lifecycle with real hooks and trusted the back of it to prose. 196 taught the roadmap reader to speak Batches (it had been blind to every roadmap written since the naming ruling); 187 equipped the repo with Enola; 188 made the End-tail disarm mechanical so a completed intent cannot keep its lock; 189 gave the graph tools real store discovery and stopped them deleting links they cannot explain; 190 ships every template and makes a project spawn verify itself; 191 taught the doctor to classify every shipped agent; 192 added a write-time Links gate and a projector that preserves rather than destroys; 193 made restore-to-v1 keep the frontmatter graph; 194 lets a release announce its own config question from the new code.

- `1.3.0` - shipped 2026-07-13; the advisor (intent 185): an advisor you summon on purpose, never instructions forced into your session. Two consultation agents, `plastic-advisor` (the real one, model `fable`, effort `xhigh`) and `plastic-faux-advisor` (model `opus`, effort `max`, carrying the frontier reasoning discipline inlined in its own body so an ordinary model reasons at frontier level), plus the `plastic-agent-advisor` skill that teaches when a consultation is worth the money, routes to the configured advisor, and sets the configuration on request. Configuration is harness-scoped on Plastic own harness keys: `advisor.claude.{default,primary,secondary}` name AGENTS (so a slot can point at your own locally registered agent) and `agents.models.claude.*` name MODELS through family aliases only, so `fable` and `opus` follow to the newest model in their family and nothing rots. Install and update ask which advisor is your default. Fixes a real leak found on the way: a literal model id in the flat `agents.models` table was written into generated Codex TOML, now impossible. Codex advisor support is parked at intent 186 until that ecosystem is evaluated in real use.
- `1.2.0` - shipped 2026-07-13; collected 33a (Codex adapter core: plastic-install --codex lays the adapted skills into ~/.agents/skills/ and injects a marked, hash-stamped Plastic section into ~/.codex/AGENTS.md, with surgical uninstall and a doctor check), 102 (the lifecycle gates ported to Codex: hooks.json registration derived from the hook registry, an apply_patch V4A envelope parser giving true pre-write vetoes, and a Ruby dispatcher driving the four existing gate and savepoint cores unchanged), and 102a (per-agent TOML generation in ~/.codex/agents/ mapping reasoning tiers to model_reasoning_effort with shape-based model overrides, fixing the dead agents copy); community-verified by 182 (the PreToolUse Bash-only claim resolved as version drift fixed in Codex 0.123.0 per PR 18391, the V4A grammar confirmed against the official apply-patch crate closing 102 residual, Tier A standing; hardening seed 184 parked); suite green at 1338 runs.
- `1.1.5` - shipped 2026-07-12; collected 179 (the unit of work is an intent, never a ticket - owner ruling recorded as one plain sentence in PLASTIC.md's What is an Intent section, and the last unit-of-work "ticket" wordings swept from the README and the roadmap and roadmap-continuing skills plus the matching eval prompt; the humanizer's support-ticket voice metaphor was judged a different sense and left; suite green at 1251 runs).
- `1.1.4` - shipped 2026-07-11; collected 149a (the dashboard boards render their four intent lists - recently worked, next work, active, future - as Markdown tables filled from cell-ready pre-escaped --data payload fields, per the owner tabular-first ruling; the 149 prose demotion stands, the classification engine, --json contract, and ASCII goldens are untouched, and the release-verify pass made the three collections_for_cwd tests hermetic, taking the suite fully green at 1251 runs).
- `1.1.3` - shipped 2026-07-11; collected 174 (the plastic-feedback channel: a user-only skill where the agent fills a sanitized, secret-redacted report of a Plastic quirk into ~/.plastic/feedback/ and the human reviews and submits it through a prefilled zalom/plastic new-issue URL - the agent has no send path by construction; ships the FeedbackReport engine and CLI with an encoded-URL 7500-byte cap and paste-free overflow, the feedback Issue Form, the PLASTIC.md offer nudge, and docs sync; born from six quirks a mihradesign delivery had no way to report, which also became intents 175-177 and 114 evidence).
- `1.1.2` - shipped 2026-07-10; closes the consistency-dividend roadmap (intent 171): 133a (actions are mandatory at every tier - the planner writes at least one real action file for every intent size, the How gate refuses execution while actions/ holds only a placeholder, the L-only wording reversed at nine sites, with the owner-requested checklist-vs-actions analytics recorded and no retroactive migration) and 85b (a deterministic skill-lint runner enforcing the skill-authoring standard mechanically - body budget, frontmatter validity, sentence- and bullet-scoped bare-pointer detection, orphan files, references one level deep - proven red and green per check, the live skill tree passing clean with no skip-list, plus a non-blocking doctor advisory; its reviewer caught and fixed two real detection leaks before approval).
- `1.1.1` - shipped 2026-07-10; collected the consistency-dividend roadmap waves 1-4 (owner-ordered early cut, roadmap intent 171 stays open for 133a and 85b): 169 (one PLASTIC_HOME sandbox seam across every entry point plus a runtime hermeticity test), 136 (every lock-repair path leaves a complete bridge), 144 (first install defaults to the stable channel), 156 (Node 24 toolchain pin and CI action bumps), 172 (the end-intent close script finally ships, with a packaging guard that fails when any skill-referenced script is missing from the manifest), 133 (the scaffolded actions/ dir survives fresh checkouts), 158a1 (/plastic-continuing returns as the bare front-door router with new project-continuing and roadmap-continuing skills and a slimmed intent-continuing), 170 (doctor agent-model drift check, never-Fable carve-out, cosmetic sweeps, future-intent researcher wired into stale triage), 155 (release lanes codified with stable-line guarantees, a tested version-agreement guard, and the intent-41 re-land playbook), 170a (A2 cutoff amnesty: 63 pre-161 ledger gaps grandfathered via a frozen allowlist, new terminals still warn), 149 (the dashboard becomes template-free prose over a flat ranked next_work payload), 134 (roadmaps gain an append-only savepoint ledger plus intent-ledger phantom detection), and 148 (the auto loop now selects work roadmap-first via roadmap-next, with an injectable ranking seam reserved for the 173 decision-systems research).
- `1.1.0` - shipped 2026-07-10; collected the manual-first roadmap (owner goal, intent 162): 158a (18 skill directories renamed group-first so each command family reads at a glance, versions became the read-only-by-default rollback command, updates self-clean renamed-away folders via the install receipt, doctor gained a stray-skill backstop that never deletes user-authored skills), 163 (the intent-speccing command: enriched Why to spec.md in deterministic steps, one fill method shared with the auto agent), 161 (the intent-ending skill plus the end-intent close script: one owned Done procedure, the missing terminal ledger bookend root-caused to releasing and fixed, all three old exit paths rerouted), 164 (intent-brainstorming, intent-planning, and intent-executing rewritten as deterministic steps a smaller model runs from the text alone, with a shared decision-table reference and evals on all three), 165 (strict-YAML-safe skill frontmatter with a regression test over all 28 skills), 168 (the code gate resolves the edited file worktree membership before the session filter, ending false blocks in parallel deliveries), 166 (/plastic-tutorial with three tracks each walked end to end by a sandboxed agent before shipping, plus the README How to use Plastic section; supersedes 137), and 159 (the agent-role map, store-side, agent surface found defect-free). Delivered by five parallel Fable-led teams through a single merge queue; beta re-lined to 1.2.0-beta.1 carrying the 41 DB layer, unreleased.
- `1.0.3` - shipped 2026-07-09; collected 158 (the user-invocable command surface: every skill frontmatter now declares user-invocable true or false per owner rulings - 20 work commands stay in the / menu, 8 maintenance skills go agent-only - with every user-facing repair instruction rerouted to /plastic-doctor natural-language phrasing, the lock skill name: fixed to plastic-lock, and the new field documented in the skill-authoring reference; owner ruled the map live by row number over a numbered decision table) and 160 (the tabular-first reporting convention in PLASTIC.md: tables required for any listing or discussion of intents and for complex data, comparisons, or multi-factor reasoning, calibrated so simple data stays prose and bullets cap at 3-5, with the nine-reason rationale as a table and one-line pointers from intent-discovery, research, and the agent report contract).
- `1.0.2` - shipped 2026-07-09; collected 157 (PLASTIC.md, the boot-injected agent conventions file, now documents the four conventions the 1.0.1 fixes introduced: the code gate's audited # plastic-ok escape scoped to Write and Edit, the spawn preamble's provisioned-worktree path with the cd fallback for non-repo launch dirs, the insight-append CLI shipping on every install/update, and new-intent's style-preserving chain wiring plus safe --intent quoting; four structural tests pin the prose; independent review APPROVE after one fix wave that corrected an escape-scope claim against the actual gate code). Docs-only fast-follow to 1.0.1.
- `1.0.1` - shipped 2026-07-09; collected 150 (hook-code-gate resolves its bridge from the edited file's path so concurrent multi-intent sessions stop misrouting edits to the newest-armed sibling, plus an audited # plastic-ok escape on the code gate), 151 (the insight-append CLI ships via the installer manifest, closing the packaging gap every dispatched agent hit), 152 (the spawn preamble carries the provisioned code worktree path with a cd fallback for sessions launched outside the repo), 154 (new-intent preserves block-style chain lists instead of corrupting them and escapes quotes/backslashes in --intent values), and 135 (project-root roadmaps/ indexed again via companion -roadmaps QMD collections with reindex wiring in the roadmap skill). First stable patch after 1.0.0; all five delivered through isolated worktrees with independent adversarial review (Batch 0 of roadmaps/intelligence.md plus its first Batch 3 entry).
- `1.0.0` (first stable) - shipped 2026-07-08; collected 143 (brought PLASTIC.md to
  operational completeness so a booting agent learns the whole surface, cleaned the install
  directions down to the stable path, descoped loop engineering and the intelligence track out
  of the stable-1-0 roadmap into a standalone-agent track, bumped the three version files).
  Strips the prerelease suffix and takes the `latest` dist-tag; closes out the stable-1-0
  roadmap. Loop engineering (intent 69 and its cluster) and the intelligence track ship later
  under the standalone-agent track (roadmaps/standalone-agent.md).
- `1.0.0-beta.39` - shipped 2026-07-07; collected 141 (dashboard JSON output made deterministic
  across json gem versions by canonicalizing empty containers, fixing the red first run of the
  new CI workflow so the README badge is green and honest). Fast-follow to beta.38.
- `1.0.0-beta.38` - shipped 2026-07-07; collected 140 (README round two from 139's researched
  proposal: an agent-first quickstart 69 words into the file, a "Plastic ships itself" proof
  section quoting five verbatim CHANGELOG/roadmap lines, four functional badges backed by a new
  GitHub Actions test workflow, "the lost thread" named problem section with a real where-was-I
  exchange, a collapsed 10-minute walkthrough, MANIFESTO.md with the two founding systems, and
  the closing line "Claude Code is powerful. Plastic makes it remember."; owner-voice pass over
  all new prose). First entry of Wave 4b (sell surface) of roadmaps/stable-1-0.md.
- `1.0.0-beta.37` - shipped 2026-07-07; collected 129 (six first-run user guides under
  docs/guides/ with a real TOC, the installer's last line and the install skill's closing line
  pointing at guide 1, pinned by a hermetic test; chained at landing to future intent 137, the
  LLM-guided tutorial), 109 (README reconciled with the shipped PLASTIC contract: the
  coordinator loop reads Build/Observe/Repeat, the lifecycle reads What/Why/How/Execute at both
  occurrences, conventions attribution names PLASTIC.md, and the resolved architecture.md
  footnote is gone; the coverage map of what a first-time reader needs in the README vs docs/
  was written at Why and consumed by 97), and 97 (the first-sight positioning: README rewritten
  around project-and-person memory - "Where you were, where you are, where you are heading" -
  the two founding systems, an honest mem0/beads contrast, a beta-only install section grounded
  in live npm dist-tags with zero @alpha left, verbatim acronyms, and the GitHub About/homepage/
  8 topics set to match; the demo GIF deferred to new intent 138). Closes Wave 4 (guides and
  first sight) of roadmaps/stable-1-0.md.
- `1.0.0-beta.36` - shipped 2026-07-07; collected 92 (the per-stage human report contract: the
  auto-mode orchestrator now briefs the human at every lifecycle stage boundary in EM-to-CTO
  voice, State/Risk/Call, impact first with one risk named and the decision left to the human;
  also carries the earlier plastic-humanizer skill from the same intent's first part), 127 (boot
  context cuts: `PLASTIC.md` split into a lighter boot-core plus an on-demand
  `PLASTIC-reference.md`, the per-prompt QMD/Serena power-tools line condensed to one line, and
  the eight auto-mode agent descriptions tightened to one sentence each, closing out intent 83's
  audit), 128 (solo-mode gate defaults: a solo user working alone no longer hits the lock and
  worktree arbitration gates as hard blocks; both relax to advisory the moment a positive,
  lock-file-based solo determination holds, and fall back to today's fail-closed behavior the
  instant any sign of parallel or team delivery appears), and 85a (seven of the heaviest skill
  bodies slimmed via progressive disclosure, each with a new reference file holding the moved
  detail, plus an em-dash cleanup in `PLASTIC.md` and `docs/architecture.md`). Suite 1023/0/0.
  Fifth cut of the stable-1.0 roadmap run.
- `1.0.0-beta.35` - shipped 2026-07-07; collected 121a (fix a quoted-redirect-target regression from 121: a quoted target like `> "app.rb"` now unwraps and is captured instead of being blanked, so the pre-How code gate blocks it the same as the unquoted form; also broadens heredoc delimiter recognition to accept a leading digit), 55 (the four lifecycle skills, install/update/uninstall/versions, now wrap a single pinned `npx -y @zalom/plastic@<channel> <verb>` call instead of re-implementing scaffolding, closing the drift-to-a-bare-npx failure mode), 118 (reconciled the What-stage activation order: the delivery lock arms first, then discovery runs under it, resolving a collision with the write-gate), 38 (a newcomer with no Ruby, or a too-old one, now gets a branded offer with the exact mise command to fix it, instead of a raw error), and 77 (guided install now asks whether to keep an existing statusline or switch to Plastic's statusline, instead of silently overwriting it). Fourth cut of the stable-1.0 roadmap run.
- `1.0.0-beta.34` - shipped 2026-07-07; collected 131 (per-intent bridge cache keying so parallel deliveries in one session stop clobbering each other s /tmp bridge and misfiring gates against the wrong intent; the six live collisions from the 2026-07-06 roadmap run, including a wrong-actor delivery, are the motivating evidence) and 125 (calmer dashboard first render: effort threshold and flag logic tuned so a real store spreads across the matrix instead of collapsing into DEFER/TRIAGE, list caps with +N more, and a hook-owned one-line systemMessage floor so the board reliably reaches the user). Third cut of the stable-1.0 roadmap run.
- `1.0.0-beta.33` - shipped 2026-07-07; collected 124a (roadmaps/ relocated out of store/ to the project root as a sibling of INDEX.md wherever INDEX.md lives, the live stable-1-0 roadmap and archived/ moved along, skill/references/template/PLASTIC.md repointed, and every roadmap log line now opens YYYY-MM-DD HH:MM UTC with existing lines retrofitted from git history; also clears the doctor warns from store/roadmaps/ being scanned as an intent dir) and 126 (doctor first-run trust: post-update doctor defaults to the fast core-sync result with the full 47-check walk behind a --full-doctor flag, reversing 56s mandatory full run; the already-shipped Ruby 4 Psych date fix and dot-dir exclusion locked in with proven regression tests). Second cut of the stable-1.0 roadmap run.
- `1.0.0-beta.32` - shipped 2026-07-06; collected 124 (roadmap delivery collections: roadmaps/{slug}.md as the delivery-side counterpart of a release, waves of parallel-safe entries with a checkable goal and an append-only log, template + plastic-roadmap skill + PLASTIC.md contract, plus the amendment adding the human-comprehension surface with checkbox waves and plain-language log lines linking outcome.md, roadmaps/archived/ for closed roadmaps, and the roadmap-as-planning-half-of-loop-engineering positioning), 121 (quote/heredoc-aware bash-gate redirect parser that fails open on ambiguous parses, killing the false positives that blocked legitimate pre-How store commits), and 130 (proportional auto-mode cycle tiers: intents sized S/M/L at Why, S/M collapse artifact depth and run one thinker for Why+How while structure, gates, independent review, and outcome.md stay identical; guided deliberately slow, auto deliberately fast). First cut of the 2026-07-06 stable-1.0 roadmap run.
- `1.0.0-beta.31` - shipped 2026-07-05; collected 119 (relocate the per-release history out of AGENTS.md into a new root CHANGELOG.md: the 20 shipped beta lines moved verbatim under ## Released, the 1.0.0/1.1.0 placeholders under ## Unreleased; AGENTS.md trimmed to the release-agnostic-intent rule folded into ### Commits and releases and repointed to CHANGELOG.md, with an explicit doc-authority boundary added to ### Documentation; plastic-releasing wired with a "Cut the CHANGELOG entry" step at version-bump time. Docs-only across 3 files, PLASTIC.md untouched. Suite 899/0/0). From 105, related to 33b/109.
- `1.0.0-beta.30` - shipped 2026-07-04; collected 111 (per-artifact delegate arbitration plus delegate visibility: an additive Claim module with per-intent-per-artifact O_EXCL claim files, a second dormant write-gate composed into hook-lock-gate, live-claim visibility and claim/release-claim verbs in plastic-lock, fail-open on stale or corrupt state; 40 new tests. Suite 899/0/0). Spawned 120 (auto-claim wiring). From 108, chain 117/120, related to 74/4a1b1/110. (Backfilled: beta.30 shipped before CHANGELOG.md existed.)
- `1.0.0-beta.29` - shipped 2026-07-04; collected 105 (retire the 91-D6 link-decisions.md convention: link-suggest --record now writes the rating/reason as a dated line in the subject intent's ## Insights, inside the same single write as the frontmatter sources/chain edge, via the reused Insights.with_entry from intent 82; append_decision renamed append_link_insight, the side-file path and "intent 91 D6" header removed; prose updated in scripts/link-suggest and skills/linking-intents/SKILL.md; the one stray link-decisions.md in intent 92's dir relocated by the curator via move-and-record; an audit confirmed no other instruction surface still teaches the convention. Suite 859/0/0). From 96 D3, related to 91/92/107/110. (Betas 25-28 shipped without individual plan entries; this line resumes the log.)
- `1.0.0-beta.24` - shipped 2026-07-02; collected 110 (route doctor's structural-junk warns to the revisions.md remedy: section_structure is now fixable:true with a fix_hint dispatching plastic-intent-curator to relocate an unsanctioned section into the intent's revisions.md via move-and-record, missing section restore/reproject; graph_i4_danglers fix_hint names the same curator/revisions.md remedy for broken-source/broken-chain refs; matching doctor-SKILL fix-hint to action table row; doctor.rb read-only contract preserved, skill applies the fix; graph_cross_store_resolution left on its rebuild-graph remedy. Closes the 107 detect to remedy loop. New hermetic test/doctor_revisions_remedy_test.rb; suite 750/0/0). Reinforced 108 during delivery (arm-stamps-ephemeral-pid lock defect plus a non-hermetic suite test clobbering the live /tmp bridge). From 107, related to 60b/19a.
- `1.0.0-beta.23` - shipped 2026-07-01; collected 107 (revisions.md structural-maintenance audit trail: an optional append-only per-intent audit file where the intent-curator relocates a misplaced section, file, or ref via a dated, rule-tagged, move-and-record entry without altering delivered meaning; template plus PLASTIC.md contract plus docs plus curator role plus two guard tests, no runtime logic since revisions.md is invisible to every validator/gate/doctor). Spawned future intents 108 (test and fix the locking mechanisms; two defects found during delivery) and 109 (audit README against the shipped PLASTIC implementation). Suite 747/0/0.
- `1.0.0-beta.22` - shipped 2026-06-30; collected 96 (hard-block work on an intent without a lock: arm_guided lock acquisition, a fail-closed L1 PreToolUse lock-gate that JSON-denies a no-lock mutating write to an active intent own dir with per-intent scope and pid/host staleness, and the /plastic-intent-starting Start procedure: lock first, confirm savepoint, ask auto/guided once, board at the latest delivered station). Suite 745/0/0.
- `1.0.0-beta.21` - shipped 2026-06-29; collected 85c (creating-skills global-hook improvements: token-reduction and propose-only self-improving pointers in references/hooks.md E7/E8, output-token axis noted as guidance-only, eval depth with real assertions plus a hermetic scaffolder test; A-G standard rule E7). Spawned future intents 100 (verifying-skills), 101 (improving-skills), 102 (portable-hooks-via-adapters), 103 (scoped-hooks-ecosystem-research), 104 (token-trimmer-hook); related 76. Suite 716/0/0.
- `1.0.0-beta.20` - shipped 2026-06-29; collected 98 (scrub the phantom CLAUDE_SESSION_ID from the whole repo: resolve_session drops its dead env fallback so the chain is explicit-stdin then CLAUDE_CODE_SESSION_ID then derived key; the five hooks that read the phantom as a live fallback swap to CLAUDE_CODE_SESSION_ID, preserving the background/headless real-id path; comment, doc, agent, and skill prose reworded to stop naming the phantom while keeping the rationale; six test files rewired to inject via CLAUDE_CODE_SESSION_ID with ambient-clear. Suite 711/0). Also carried 85 already-landed creating-skills code (b74bf43), completed but not yet cut.
- `1.0.0-beta.19` - shipped 2026-06-25; collected 92 (plastic-humanizer skill: clean authored prose, remove AI tells and slop from docs/specs/outcomes/READMEs).
- `1.0.0-beta.18` - shipped 2026-06-25; collected 89a (operation-based retrieval gate: only CONTENT SEARCH over a store is hard-gated to QMD; reads and structural ops like Read/cat/find/ls/Glob are always allowed, including over the store; the Serena hard-gate is removed and code navigation stays a soft UserPromptSubmit mandate with content grep over code allowed; a tier-b warn fires when QMD is present but its freshness probe breaks; block reason teaches the semantic not score-based # qmd-ok fallback. Suite 712/0. Spec in intent 89a, policy map in intent 89, from grill-me brainstorm).
- `1.0.0-beta.12` - shipped 2026-06-23; collected the 73c worktree-isolation chain (73c decision + 73c1/73c2/73c3). Plastic-supplied per-intent isolation: new scripts/lib/worktree.rb provisions a mandatory code worktree plus a consistency store worktree at arm time via `git -C <repo-from-projects.yml>` (cwd-independent, closing the one genuine harness GAP from 73 matrix); the session bridge becomes a single-owner delivery lock (owner_session/pid/acquired_at/host) with stale-lock reclaim via process liveness (superseded by intent 108's session-keyed lease lock); Bridge.worktree_gate_decision hard-blocks code edits outside the intent worktree and non-owner edits to live-locked active intents, composed into hook-code-gate with a logged fail-open matrix; Worktree.finish merges-then-removes on the release path and ensure_gitignored keeps .worktrees/ and .claude/worktrees/ out of the git index; single-owner plus mandatory-worktree doctrine added to PLASTIC.md and AGENTS.md. Suite 606/0/0. Spawned 73c1a (isolate bridge writes in tests).
- `1.0.0-beta.11` - shipped 2026-06-22; collected 73b (fix skill session-id resolution prose: plastic-auto arm/disarm examples pass CLAUDE_CODE_SESSION_ID, not the always-empty CLAUDE_SESSION_ID; headless notes corrected). Owns 73 follow-up #1.
- `1.0.0-beta.10` - shipped 2026-06-22; collected 80 (bridge cleanup on work-done). Replaced intent 67's 48h age-window purge with terminal-state cleanup in core bridge.rb: purge_done_bridges removes a /tmp/plastic-*.json bridge only when its intent is no longer Active in its store's INDEX (keyed by intent.id plus intent.store via new intent_active?); active intents' and the current session's bridges are never purged (continuation signal plus anti-collision lock); removed PURGE_AGE_SECONDS; arm/disarm repointed. Bridge and savepoint are independent mechanisms (not dependent on 81). One-time live clean slate took /tmp from 451 to 1. Folds in 73 follow-up #2; 73 follow-up #1 is owned by sibling 73b.
- `1.0.0-beta.9` - shipped 2026-06-22; collected 49 and 72 (store-wide knowledge-graph consistency). 49: deterministic rebuild-graph tool plus a graph_cross_store_resolution doctor check (one-directional I1/I3/I4, preserve I2, cross-store relocation resolution that fixed the global:24 id-reuse hazard). 72: fence-aware project-links tool plus a graph_links_projection doctor check; canonical ## Links projection store-wide (id--slug target, full intent label, sources-first mandatory ordering); the I5 rule codified in PLASTIC.md, the concept doc, and the references; new-intent now emits canonical Links at creation so drift is prevented at the source. Spawned 82 (improve Insights: exact timestamps plus tightened background-agent delivery).
- `1.0.0-beta.8` - shipped 2026-06-22; collected 79 (per-session statusline via the session Bridge: the statusline resolves the work-unit from the current session's live bridge instead of the shared newest-savepoint heuristic, so parallel sessions stop overwriting each other's line; added a CLAUDE_CODE_SESSION_ID fallback to Bridge.resolve_session). Spawned 80 (bridge cleanup on work-done) and 81 (savepoint state from the ledger).
- `1.0.0-beta.7` - shipped 2026-06-22; collected 66c1 (extends 66c: the plastic-intent-curator async QMD reindex now fires on any terminal-state transition - move to Completed OR Abandoned - not completion only; prose-wiring, no new Ruby).
- `1.0.0-beta.6` - shipped 2026-06-22; collected 78 (fix the installer manifest gap from 66b: power_tools.rb was never added to InstallerCore#core_files, so it never installed into ~/.plastic and the UserPromptSubmit qmd hook raised a LoadError on every prompt; adds the manifest entry plus a regression-guard test asserting every scripts/lib/*.rb is in the manifest).
- `1.0.0-beta.5` - shipped 2026-06-21; collected the QMD/Serena power-tools mandate cluster: 66b (mandated QMD+Serena harness, detect-then-degrade), 66c (async reindex on completion), 66a (QMD-first search step across 9 store-searching skills + qmd-sync search verb).
- `1.0.0-beta.4` - shipped 2026-06-21; collected 59 (Plastic owns the statusline: project-aware, colored, version+path last). Spawned 77 (guided-install statusline choice).
- `1.0.0-beta.3` - shipped; collected 74 (mandatory structured agent completion reports + deterministic fallback).
- `1.0.0-beta.2` - shipped; collected 68 (sources-vs-chain semantics). The other planned blockers (58, 49, 39, 66a) slipped to later cuts.
- `1.0.0-beta.1` - shipped 2026-06-19. Promoted the alpha line; feature-complete core.
